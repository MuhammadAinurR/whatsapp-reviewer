const { TIMEOUT, COMPANY } = require('./constants');
const { INTERVIEW_CONFIG, AGENTS, STAGES } = require('./interview');

module.exports = {
  TIMEOUT,
  COMPANY,
  INTERVIEW_CONFIG,
  AGENTS,
  STAGES
}; 